const money=n=>"₹"+Number(n).toLocaleString("en-IN");async function stats(){let d=await(await fetch("/api/stats")).json();pc.textContent=d.products;sc.textContent=d.stock;oc.textContent=d.orders;sales.textContent=money(d.sales)}async function load(){let a=await(await fetch("/api/products")).json();rows.innerHTML="<tr><th>ID</th><th>SKU</th><th>Name</th><th>Category</th><th>Price</th><th>Stock</th></tr>"+a.map(p=>`<tr><td>${p.id}</td><td>${p.sku}</td><td>${p.name}</td><td>${p.category}</td><td>${money(p.price)}</td><td>${p.stock}</td></tr>`).join("");stats()}pf.onsubmit=async e=>{e.preventDefault();let o=Object.fromEntries(new FormData(pf));let r=await fetch("/api/products",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(o)}),d=await r.json();if(!r.ok)return alert(d.error);pf.reset();load()};async function orders(){let a=await(await fetch("/api/orders")).json();orows.innerHTML="<tr><th>Order</th><th>Customer</th><th>Phone</th><th>Total</th><th>Payment</th><th>Status</th></tr>"+a.map(o=>`<tr><td>${o.order_no}</td><td>${o.customer_name}</td><td>${o.phone}</td><td>${money(o.total)}</td><td>${o.payment_method}</td><td>${o.order_status}</td></tr>`).join("")}load();orders();